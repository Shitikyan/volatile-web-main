import { IEnvironment } from './types';

export const environment: IEnvironment = {
  production: true,
  languages: ['EN', 'RU', 'AM'],
  defaultLanguage: 'EN',
  darkTheme: 'dark',
  defaultTheme: 'light',
};
